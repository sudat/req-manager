
現状のリポジトリを総合的に分析し、①prd.mdに記載はあるがまだモックページが無い箇所を整理して。整理する際にはあなたの見解としてprd.mdのとおり追加するべき、または追加不要・修正すべきといった意見もつけて。②prd.mdに記載がないが現状のモックページある箇所を整理して。現状のモックページの仕様を元に修正すべきといった意見があれば教えて。

方針：PRDの画面一覧（2章）を軸に、リポジトリ内の現状ルーティング（app/**/page.*）とモック画面を棚卸しします。次に「PRDにあるが未実装」「PRDにないが存在」を突合して、追加/不要/修正の私見まで短く添えます。まずはページ一覧を機械的に抽出します。
いい発見：app/ 直下のルーティング一覧が取れました。PRDメニューの多く（dashboard/query/business/tickets/baseline/export/settings）は既にNext.js側でページが存在します。一方でPRDにある「グラフビュー」「概念辞書」は見当たらず、逆にPRDにない srf と ideas が独立メニューとして存在しているので、ここを重点的に中身を読んで突合します。
Nice、/query の結果カードが概念リンクとして /ideas/:id に飛ぶ実装になっていて、PRDの「概念辞書」が別名で既に作られている可能性が高いです。次は Sidebar と ideas/srf/tickets/:id などを読んで、PRDの画面名と実際のページを1対1でマッピングします。
Aha：PRDメニューの「概念辞書」は現状 /ideas として実装済みです（Sidebarで明示）。一方でPRDの「ベースライン履歴の詳細（版ごと）」や srf/create のように、一覧から遷移リンクはあるのにページが存在しない箇所が見えてきました。次は不足ルート（baseline/[version], business/ai修正指示, graph view 等）を確定させます。
🟢### ① PRDにあるが未整備（モック/ページ不足）
グラフビュー: /graph系なし。PRD通り追加推奨（価値中核）。
ベースライン履歴の詳細/差分/新規版: /baseline/[version]等なし（一覧のみ）。追加推奨。
業務一覧のAI修正指示ページ: モックHTMLはあるがNext側なし。追加推奨（PRD導線の要）。
要件用AI修正指示ページ: 独立ページなし（編集/手動追加はあるが別物）。PRD通り追加推奨。
変更要求詳細の「変更内容生成」「レビュー・承認（コメント）」: 現状UI不足。PRD優先度高なら追加、低ならPRD側を現状に合わせ簡略化も可。
② PRDにないが現状ある
システム機能（/srf）: PRD外だが要件→実装参照/設計書管理に効く。残すならPRDへ追記推奨。
設定の細分（/settings/llm,/settings/notification,/settings/domains）: PRDの「設定」を具体化していて良い。PRD側に小項目追記推奨。