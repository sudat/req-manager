# タブ名変更：プロジェクト設定 → プロジェクト調査設定

## 目的
PRD第6.15節の用語「プロジェクト調査設定」と整合性を取るため、設定画面のタブ名を変更する。

## 変更内容

### 変更ファイル
| ファイル | 変更箇所 | 変更内容 |
|---------|---------|---------|
| `app/(with-sidebar)/settings/page.tsx` | 28行目 | `プロジェクト設定` → `プロジェクト調査設定` |

### 具体的な変更
```tsx
// 変更前
<TabsTrigger value="project" className="px-6">プロジェクト設定</TabsTrigger>

// 変更後
<TabsTrigger value="project" className="px-6">プロジェクト調査設定</TabsTrigger>
```

## 変更しないもの
- `value="project"` (内部値) - 変更不要
- コンポーネント名 (`ProjectSettingsContent`) - 別途変更検討が必要だが、今回のスコープ外
- 型定義・データ構造 - 変更なし

## 難易度評価
**難易度: ★☆☆**
- 根拠: 1ファイル、1行変更、依存なし
- リスク: なし（表示文字列のみの変更）

## 検証方法
1. 設定画面 (`/settings`) にアクセス
2. タブ名が「プロジェクト調査設定」と表示されていることを確認
3. タブ切り替えが正常に動作することを確認

## 実装コマンド
```bash
# ファイル編集（Editツールを使用）
# その後、動作確認
```
